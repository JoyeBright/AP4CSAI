# Assign a string to the variable
a_string_variable = "Hello world!"
print(type(a_string_variable), id(a_string_variable))

# Reassign the variable to an integer
a_string_variable = 42
print(type(a_string_variable), id(a_string_variable))